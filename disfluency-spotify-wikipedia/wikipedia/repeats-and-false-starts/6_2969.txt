One or more consecutive groups of zero value may be replaced replaced replaced replaced replaced replaced replaced with a single empty group using two consecutive colons (: : : : : : : : ), but the substitution may only be applied once once once once once once once in the address, because multiple occurrences would create an ambiguous representation.