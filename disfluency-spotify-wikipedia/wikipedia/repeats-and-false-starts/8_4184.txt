The meaning of each of them can be represented represented represented represented represented represented represented represented represented by a certain fuzzy set. One might define define define define define define define define define the glass as being 0. 7 empty and 0. 3 full.