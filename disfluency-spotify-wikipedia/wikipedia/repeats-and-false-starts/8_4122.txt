The image rendering takes place in Prisma labs's servers servers servers servers servers servers servers servers servers and it utilizes neutral network and artificial artificial artificial artificial artificial artificial artificial artificial artificial intelligence to add artistic effect and the result in delivered back to the user's phone.