One or more consecutive groups of zero value may may be replaced with a single empty group using two consecutive consecutive colons (: : ), but the substitution may only be be applied once in the address, because multiple occurrences would create an ambiguous representation.