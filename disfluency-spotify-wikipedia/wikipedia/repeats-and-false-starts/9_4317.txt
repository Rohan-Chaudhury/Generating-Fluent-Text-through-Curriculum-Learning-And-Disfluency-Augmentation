The reference Cool compiler is written in C++, built entirely entirely entirely entirely entirely entirely entirely entirely entirely entirely on public domain tools; it generates code for a MIPS simulator, SPIM.