The pwd command (print working directory) is used to to to to to to print the name of current working directory, if if if if if if the shell prompt does not show this, the user can find their place in the directory tree.