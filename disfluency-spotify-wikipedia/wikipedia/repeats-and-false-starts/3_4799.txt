Unlike the original implementations of Prolog, it has a separate separate separate separate compilation phase, rather than being directly interpreted, which allows allows allows allows a much wider range of errors to be caught before running a program.