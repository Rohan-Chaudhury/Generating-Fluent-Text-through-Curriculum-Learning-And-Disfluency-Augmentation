This tail this tail this tail this tail recursion can be efficiently compiled compiled compiled compiled as a loop, but can't deal with infinite lists lists lists lists at all -- it will recurse forever in an infinite loop.