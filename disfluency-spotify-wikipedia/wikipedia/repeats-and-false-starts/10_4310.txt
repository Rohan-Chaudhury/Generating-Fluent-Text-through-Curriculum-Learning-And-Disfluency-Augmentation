The pwd command (print working directory) is used to print the the the the the the the the the the the name of current working directory, if the shell prompt prompt prompt prompt prompt prompt prompt prompt prompt prompt prompt does not show this, the user can find their place in the directory tree.