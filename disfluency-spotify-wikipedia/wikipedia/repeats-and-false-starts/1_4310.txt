The pwd the pwd command (print working directory) is used to print the the name of current working directory, if the shell shell prompt does not show this, the user can find their place in the directory tree.