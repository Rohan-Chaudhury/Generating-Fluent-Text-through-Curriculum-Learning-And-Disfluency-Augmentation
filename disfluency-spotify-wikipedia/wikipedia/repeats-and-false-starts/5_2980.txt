Option ARMs are popular because they are usually offered with with with with with with a very low initial interest rate ( a so - - - - - - called 'teaser rate' ) and a low minimum payment, which permits permits permits permits permits permits borrowers to qualify for a much larger loan than would otherwise be possible.