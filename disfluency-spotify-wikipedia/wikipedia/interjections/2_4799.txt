Unlike the original implementations of Prolog, it has a separate compilation you know um phase, rather than being directly interpreted, which allows a much wider so I mean range of errors to be caught before running a program.