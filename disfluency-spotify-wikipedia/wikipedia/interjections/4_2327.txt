Its worst - case time complexity is, but on average like like I mean um its performance is or even depending on the random um well um so distribution on automata chosen to model the algorithm's average - case behavior.