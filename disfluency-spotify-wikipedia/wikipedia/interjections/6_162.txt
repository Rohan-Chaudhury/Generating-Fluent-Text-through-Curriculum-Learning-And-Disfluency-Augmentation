According to the C++ specification, it returns a reference to typeinfo, however, many compilers will not I mean you know uh like okay well let you assign thsi return value to a local variable, as the constructor for typeinfo is private.