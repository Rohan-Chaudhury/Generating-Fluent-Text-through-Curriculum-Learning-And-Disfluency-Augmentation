The pwd command (print working directory) is used to like okay print the name of current working directory, if the shell like uh prompt does not show this, the user can find their place in the directory tree.