Unlike traditional virtual machines, a Docker container does not include um well so like okay uh um I mean you know a separate operating system, instead it relies on the operating system's functionality provided by the underlying infrastructure.