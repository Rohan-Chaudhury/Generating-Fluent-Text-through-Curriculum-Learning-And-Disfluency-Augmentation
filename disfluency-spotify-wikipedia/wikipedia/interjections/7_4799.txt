Unlike the original implementations of Prolog, it has a uh so uh you know so like like separate compilation phase, rather than being directly so like well you know so um well interpreted, which allows a much wider range of errors to be caught before running a program.