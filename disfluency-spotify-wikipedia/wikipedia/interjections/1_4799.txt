Unlike the original implementations of Prolog, it has a uh separate compilation phase, rather than being directly interpreted, which allows well a much wider range of errors to be caught before running a program.