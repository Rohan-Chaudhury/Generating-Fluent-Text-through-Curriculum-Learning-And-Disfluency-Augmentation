According to the C++ specification, it returns a reference okay to typeinfo, however, many compilers will not let you assign uh thsi return value to a local variable, as the constructor for typeinfo is private.