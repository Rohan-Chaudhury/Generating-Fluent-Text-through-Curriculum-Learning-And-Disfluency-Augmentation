One or more consecutive groups of zero value may be replaced with a single well you know like I mean empty group using two consecutive colons (: : ), but the substitution may so okay like okay only be applied once in the address, because multiple occurrences would create an ambiguous representation.