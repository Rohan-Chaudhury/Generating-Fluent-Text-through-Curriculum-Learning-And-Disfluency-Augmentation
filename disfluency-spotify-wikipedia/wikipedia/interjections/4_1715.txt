However, it can be difficult to estimate 'K' well like so like i and 'K' i' accurately from such plots, so like you know like like it is advisable to estimate these constants using more reliable nonlinear regression methods, as described above.