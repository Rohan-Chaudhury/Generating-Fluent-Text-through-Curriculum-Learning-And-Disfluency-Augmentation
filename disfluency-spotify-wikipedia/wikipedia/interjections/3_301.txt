Allocating frequently used variables to registers can be critical uh like um to a program's performance; this register allocation is performed either by like so okay a compiler in the code generation phase, or manually by an assembly language programmer.