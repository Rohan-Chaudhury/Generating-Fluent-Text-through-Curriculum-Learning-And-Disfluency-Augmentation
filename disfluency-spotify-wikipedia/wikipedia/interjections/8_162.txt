According to the C++ specification, it returns a reference to typeinfo, however, many compilers I mean well I mean okay so like like uh will not let you assign thsi return value to a local variable, as the constructor for typeinfo is private.