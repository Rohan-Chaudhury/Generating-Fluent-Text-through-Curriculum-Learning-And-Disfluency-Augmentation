According to the C++ specification, it returns a reference to I mean so I mean typeinfo, however, many compilers will not let you know you know so you assign thsi return value to a local variable, as the constructor for typeinfo is private.