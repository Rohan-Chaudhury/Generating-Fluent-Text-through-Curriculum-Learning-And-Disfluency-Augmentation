According to the C++ specification, it returns a reference so you know um uh to typeinfo, however, many compilers will not let you assign okay okay uh um thsi return value to a local variable, as the constructor for typeinfo is private.