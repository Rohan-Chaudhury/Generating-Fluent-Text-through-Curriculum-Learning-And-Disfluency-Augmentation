Unlike the original implementations of Prolog, it has a separate um um um like okay compilation phase, rather than being directly interpreted, which allows um um well I mean so a much wider range of errors to be caught before running a program.