Unlike the original implementations of Prolog, it has a uh like well separate compilation phase, rather than being directly interpreted, which allows I mean um um a much wider range of errors to be caught before running a program.