The pwd command (print working directory) is used to print so the name of current working directory, if the I mean shell prompt does not show this, the user can find their place in the directory tree.