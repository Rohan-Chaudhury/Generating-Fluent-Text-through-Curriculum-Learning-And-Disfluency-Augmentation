The pwd command (print working directory) is used to print so like you know like so like I mean the name of current working directory, if the shell so so okay uh so okay well prompt does not show this, the user can find their place in the directory tree.