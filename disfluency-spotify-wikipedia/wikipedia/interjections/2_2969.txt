One or more consecutive groups of zero value may be um um replaced with a single empty group using two consecutive colons you know um (: : ), but the substitution may only be I mean um applied once in the address, because multiple occurrences would create an ambiguous representation.