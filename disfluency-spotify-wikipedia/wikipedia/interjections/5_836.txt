Data input required intermediate processing via punched paper you know like um well so tape or card and separate input to a repetitive, labor intensive task, removed from user control and error - prone.