The pwd command (print working directory) is used to print okay so like okay um okay the name of current working directory, if the shell I mean okay so uh so like prompt does not show this, the user can find their place in the directory tree.