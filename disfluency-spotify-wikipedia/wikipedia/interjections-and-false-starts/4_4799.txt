Unlike the unlike the unlike the unlike the unlike the original implementations of Prolog, it has a like um so I mean separate compilation phase, rather than being directly interpreted, which so so um well allows a much wider range of errors to be caught before running a program.