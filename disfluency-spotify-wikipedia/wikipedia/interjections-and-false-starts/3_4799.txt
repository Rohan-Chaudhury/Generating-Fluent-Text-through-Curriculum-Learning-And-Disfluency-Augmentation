Unlike the unlike the unlike the unlike the original implementations of Prolog, it has a okay okay well separate compilation phase, rather than being directly uh so okay interpreted, which allows a much wider range of errors to be caught before running a program.