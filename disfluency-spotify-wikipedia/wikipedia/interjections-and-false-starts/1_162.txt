According to according to the C++ specification, it returns a reference to well typeinfo, however, many compilers will not let you assign thsi return okay value to a local variable, as the constructor for typeinfo is private.