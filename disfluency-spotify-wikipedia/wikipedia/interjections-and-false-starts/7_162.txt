According to the C++ specification, it returns a reference okay um um um so um I mean to typeinfo, however, many compilers will not let so so you know so I mean you know so you assign thsi return value to a local variable, as the constructor for typeinfo is private.