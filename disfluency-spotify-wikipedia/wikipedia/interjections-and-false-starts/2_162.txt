According to the C++ specification, it returns a reference to typeinfo, however, many compilers will not let like okay you assign thsi return value to a local variable, as the constructor for typeinfo is private.