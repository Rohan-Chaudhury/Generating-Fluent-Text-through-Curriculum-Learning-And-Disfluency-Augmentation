The pwd command (print working directory) is used to okay you know like so um like like print the name of current working directory, if the well so well so so uh so shell prompt does not show this, the user can find their place in the directory tree.