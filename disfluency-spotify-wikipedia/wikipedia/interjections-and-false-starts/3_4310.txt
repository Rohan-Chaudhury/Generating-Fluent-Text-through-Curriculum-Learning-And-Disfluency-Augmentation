The pwd command (print working directory) is used to print I mean okay um the name of current working directory, if the shell so uh well prompt does not show this, the user can find their place in the directory tree.