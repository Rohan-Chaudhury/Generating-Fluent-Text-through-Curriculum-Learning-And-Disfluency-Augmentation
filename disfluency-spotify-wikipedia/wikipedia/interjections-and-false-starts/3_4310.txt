The pwd the pwd the pwd the pwd command (print working directory) is used to print the name of I mean so like current working directory, if the shell prompt does not show this, the user can find their place in the directory tree.