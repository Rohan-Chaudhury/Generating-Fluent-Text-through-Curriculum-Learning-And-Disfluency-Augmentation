The graph the graph the graph the graph the graph of the exponential distribution, called the probability density I mean okay so so function (PDF), is specified in terms of lambda (events per unit time) and x (time).