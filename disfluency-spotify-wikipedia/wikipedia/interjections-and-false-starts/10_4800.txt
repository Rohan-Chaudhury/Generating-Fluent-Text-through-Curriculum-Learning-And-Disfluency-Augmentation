Unlike traditional virtual machines, a Docker container does not include okay like so um um so like okay um okay a separate operating system, instead it relies on the operating system's functionality provided by the underlying infrastructure.