This tail this tail this tail this tail this tail this tail this tail recursion can be efficiently compiled as a well so uh you know okay so loop, but can't deal with infinite lists at all -- it will recurse forever in an infinite loop.