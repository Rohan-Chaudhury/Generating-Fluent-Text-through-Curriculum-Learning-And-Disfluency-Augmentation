It replaces it replaces it replaces it replaces it replaces it replaces it replaces the standard programming library provided by the operating system for malloc and other you know okay so um like um software with its own versions which help the programmer detect buffer overflow and other critical programming issues.