This tail this tail this tail this tail this tail this tail this tail this tail recursion can be efficiently compiled well like I mean so like so I mean as a loop, but can't deal with infinite lists at all -- it will recurse forever in an infinite loop.