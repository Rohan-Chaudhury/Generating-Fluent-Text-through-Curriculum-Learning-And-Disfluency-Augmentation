Data input required intermediate processing via punched paper okay so so so okay so uh uh tape or card and separate input to a repetitive, labor intensive task, removed from user control and error - prone.