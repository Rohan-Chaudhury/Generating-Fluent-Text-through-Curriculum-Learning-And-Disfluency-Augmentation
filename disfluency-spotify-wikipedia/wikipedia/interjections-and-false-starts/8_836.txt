Data input required intermediate processing via punched paper tape or card and separate okay I mean uh okay like so so um input to a repetitive, labor intensive task, removed from user control and error - prone.