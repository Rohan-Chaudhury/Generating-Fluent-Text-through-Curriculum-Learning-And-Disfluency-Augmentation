Unlike the original implementations of Prolog, it has well a separate compilation phase, rather than being directly interpreted, so which allows a much wider range of errors to be caught before running a program.