The pwd command (print working directory) is used to print the name um um okay so of current working directory, if the shell prompt um so uh uh does not show this, the user can find their place in the directory tree.