One or more consecutive groups of zero value like may be replaced with a single empty group using two you know consecutive colons (: : ), but the substitution may only um be applied once in the address, because multiple occurrences would create an ambiguous representation.