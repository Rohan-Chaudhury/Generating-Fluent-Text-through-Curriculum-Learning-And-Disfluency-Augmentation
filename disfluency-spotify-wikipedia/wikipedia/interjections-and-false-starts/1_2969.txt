One or more consecutive groups of zero value may be replaced with a so single empty group using two consecutive colons (: : ), but the substitution may um only be applied once in the address, because multiple occurrences would create an ambiguous representation.