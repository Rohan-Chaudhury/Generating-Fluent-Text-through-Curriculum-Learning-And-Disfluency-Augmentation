If the subtrahend, 'y', has fewer digits than the the you know so like um the the the minuend, 'x', leading zeros okay so uh like must be added, added, added, added, added, which will become leading nines when the complement is taken.