It is not acceptable to delete information from an article because it spoils the plot like um um like so so you know if if if if if if if if that would interfere with, encyclopedic tone, completeness, or any other element of article quality (for example, the).