The reference Cool compiler is written in C++, built entirely I mean well I mean um so entirely entirely entirely entirely entirely on public domain tools; it generates code for a MIPS simulator, SPIM.