Unlike the original implementations of Prolog, it has a separate compilation phase, rather rather um um okay okay rather rather rather than being directly interpreted, which allows a much wider range of errors to be caught before running a program.