Unlike traditional virtual machines, a Docker container does I mean okay not not not include a separate operating system, instead it like you know relies relies relies on the operating system's functionality provided by the underlying infrastructure.