Unlike the original implementations of Prolog, it has a separate compilation phase, rather I mean um than being being being directly interpreted, which allows a much wider range of errors to be caught before running a program.