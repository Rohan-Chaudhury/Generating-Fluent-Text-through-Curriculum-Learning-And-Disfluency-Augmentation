Unlike the original implementations of Prolog, it has a so I mean separate compilation compilation compilation phase, rather than being directly I mean well interpreted, which allows a much wider range of errors to be caught before running a program.