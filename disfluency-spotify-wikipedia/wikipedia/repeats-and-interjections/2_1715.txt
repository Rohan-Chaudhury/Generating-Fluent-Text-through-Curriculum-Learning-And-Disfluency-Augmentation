However, it can be difficult to estimate 'K' 'K' like um 'K' i and 'K' i' accurately from such plots, so it so okay it it is advisable to estimate these constants using more reliable nonlinear regression methods, as described above.