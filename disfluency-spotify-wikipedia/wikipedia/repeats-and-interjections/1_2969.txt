One or more consecutive groups of zero value like value may be replaced with a single empty group using like two two consecutive colons (: : ), but the substitution um substitution may only be applied once in the address, because multiple occurrences would create an ambiguous representation.