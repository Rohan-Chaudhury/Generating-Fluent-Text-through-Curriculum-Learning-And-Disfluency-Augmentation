According to the C++ specification, it returns returns a reference so to typeinfo, however, many compilers will not not let you know you assign thsi return value to a local variable, as the constructor for typeinfo is private.