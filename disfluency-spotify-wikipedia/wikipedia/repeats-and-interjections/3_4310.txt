The pwd command (print working directory) is used to print print print so like um print the name of current working directory, if the shell like okay okay prompt prompt prompt prompt does not show this, the user can find their place in the directory tree.