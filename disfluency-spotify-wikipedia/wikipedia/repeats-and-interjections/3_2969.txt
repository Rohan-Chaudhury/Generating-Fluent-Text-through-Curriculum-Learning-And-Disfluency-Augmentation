One or more consecutive groups of zero value may be replaced with a single single single you know like uh single empty group using two consecutive colons (: : ), but the substitution may so um I mean only be applied once in the address, because multiple occurrences would create an ambiguous representation.