One or more consecutive groups of zero value may be well so um be be be replaced with a single empty group um okay so using two two two two consecutive colons (: : I mean um uh ), but the substitution may only only only only be so like okay applied once in the address, because multiple occurrences would create an ambiguous representation.