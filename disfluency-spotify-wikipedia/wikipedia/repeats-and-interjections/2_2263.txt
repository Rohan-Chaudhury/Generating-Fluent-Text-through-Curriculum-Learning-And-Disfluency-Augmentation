It replaces the standard programming library provided by the the um um the operating system for malloc and other software with with so uh with its own versions which help the programmer detect buffer overflow and other critical programming issues.