One or more consecutive groups of zero value may be you know like be be replaced with a single empty group using two um okay consecutive consecutive consecutive colons (: : ), but the like like substitution may only only only be applied once in the address, because multiple occurrences would create an ambiguous representation.