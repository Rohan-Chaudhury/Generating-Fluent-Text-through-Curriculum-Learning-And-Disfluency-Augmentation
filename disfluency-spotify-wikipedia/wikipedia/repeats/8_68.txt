A fee simple determinable is an estate that will end automatically if if if if if if if if if the condition of the grantor occurs and the interest interest interest interest interest interest interest interest interest will revert to the grantor or the heirs of the grantor.