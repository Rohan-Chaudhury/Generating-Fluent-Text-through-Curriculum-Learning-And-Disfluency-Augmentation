One or more consecutive groups of zero zero zero zero zero zero value may be replaced with a single empty group group group group group group using two consecutive colons (: : ), but but but but but but the substitution may only be applied once in the address, because multiple occurrences would create an ambiguous representation.