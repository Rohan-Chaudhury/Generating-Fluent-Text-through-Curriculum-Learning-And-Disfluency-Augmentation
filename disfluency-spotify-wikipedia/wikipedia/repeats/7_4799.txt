Unlike the original implementations of Prolog, it has a a a a a a a a separate compilation phase, rather than being directly interpreted, which which which which which which which which allows a much wider range of errors to be caught before running a program.