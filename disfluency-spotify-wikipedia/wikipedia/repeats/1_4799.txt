Unlike the original implementations of Prolog, it has a separate separate compilation phase, rather than being directly interpreted, which which allows a much wider range of errors to be caught before running a program.