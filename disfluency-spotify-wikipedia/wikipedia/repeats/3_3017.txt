PSR J1614 -- 2230 spins on its axis roughly roughly roughly roughly 317 times per second, corresponding to a period of 3. 15 milliseconds, milliseconds, milliseconds, milliseconds, and like all pulsars, emits radiation in a beam, much like a lighthouse.