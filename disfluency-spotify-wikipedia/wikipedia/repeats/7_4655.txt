This tail recursion can be efficiently compiled compiled compiled compiled compiled compiled compiled compiled as a loop, but can't deal with infinite lists at at at at at at at at all -- it will recurse forever in an infinite loop.