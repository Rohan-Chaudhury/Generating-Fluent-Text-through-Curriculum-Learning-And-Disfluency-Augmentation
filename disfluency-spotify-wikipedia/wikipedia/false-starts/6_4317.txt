The reference the reference the reference the reference the reference the reference the reference Cool compiler is written in C++, built entirely on public domain tools; it generates code for a MIPS simulator, SPIM.