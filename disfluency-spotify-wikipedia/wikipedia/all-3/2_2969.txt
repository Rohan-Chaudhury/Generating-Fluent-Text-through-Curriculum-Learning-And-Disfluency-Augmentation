One or one or one or more consecutive groups groups groups of zero value may like okay be replaced with a a a single empty group using two so I mean consecutive consecutive consecutive colons (: : ), but the substitution may only be be be okay well applied once in the address, because multiple occurrences would create an ambiguous representation.