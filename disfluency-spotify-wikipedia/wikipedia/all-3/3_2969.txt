One or more consecutive groups of zero value may be replaced with a single single single single empty you know so so group using two consecutive colons (: : ), but but but but the substitution may only be applied once in the address, because multiple occurrences would create an ambiguous representation.