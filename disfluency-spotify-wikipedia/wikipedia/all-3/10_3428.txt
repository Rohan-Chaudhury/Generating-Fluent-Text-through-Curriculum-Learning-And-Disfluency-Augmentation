Since the map is already displayed, there is no actual exploration to be done; instead instead instead instead instead instead instead instead instead instead instead like well I mean like you know so uh well like so players are rewarded for being the first to reach different regions and return safely.