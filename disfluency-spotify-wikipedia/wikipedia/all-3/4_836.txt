Data input required intermediate processing via punched paper tape or card and separate okay okay okay okay okay uh um uh input to a repetitive, labor intensive task, removed from user control and error - prone.