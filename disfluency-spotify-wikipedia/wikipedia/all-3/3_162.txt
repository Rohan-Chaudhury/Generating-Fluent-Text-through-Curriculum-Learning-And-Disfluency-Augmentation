According to according to according to according to the C++ C++ C++ C++ specification, it returns a reference to well okay um typeinfo, typeinfo, typeinfo, typeinfo, however, many compilers will not let you assign so you you you you know so thsi return value to a local variable, as the constructor for typeinfo is private.