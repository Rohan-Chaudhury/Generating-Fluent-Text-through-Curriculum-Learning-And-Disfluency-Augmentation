PSR J1614 -- 2230 spins on its axis roughly roughly roughly roughly 317 well okay like times per second, corresponding to to to to a period of 3. 15 so um you know milliseconds, milliseconds, milliseconds, milliseconds, and like all pulsars, emits radiation in a beam, much like a lighthouse.