Unlike traditional virtual machines, a Docker container does not include include include I mean well a separate operating system, instead instead instead it relies on the operating system's functionality provided by the underlying infrastructure.