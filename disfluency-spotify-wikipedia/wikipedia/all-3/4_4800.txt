Unlike traditional virtual machines, a Docker container does not not not not not um like um well include a separate operating system, instead instead instead instead instead it relies on the operating system's functionality provided by the underlying infrastructure.