Its worst - case time complexity is, but on like like average its performance is or even depending on the random random um distribution on automata chosen to model the algorithm's average - case behavior.