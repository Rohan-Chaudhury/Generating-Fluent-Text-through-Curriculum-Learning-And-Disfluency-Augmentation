The pwd command (print working directory) is used used used to so you know print the name of current working directory, directory, directory, if the so uh shell prompt does not show this, the user can find their place in the directory tree.