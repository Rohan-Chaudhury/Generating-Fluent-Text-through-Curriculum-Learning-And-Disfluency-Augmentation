Unlike the unlike the unlike the unlike the the the the the the the unlike the unlike the unlike the original implementations implementations implementations implementations implementations implementations implementations of Prolog, it has a well I mean uh um okay you you you you you you you know separate compilation phase, rather than being directly well you know okay okay okay okay okay okay okay okay you know uh interpreted, which allows a much wider wider wider wider wider wider wider range of errors to be caught before running a program.