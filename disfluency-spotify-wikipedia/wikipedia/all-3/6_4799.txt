Unlike the original implementations of Prolog, it has a separate compilation phase, rather than than than than than than than being directly okay so well I mean you know um interpreted, which allows a much wider range of errors to be caught before running a program.