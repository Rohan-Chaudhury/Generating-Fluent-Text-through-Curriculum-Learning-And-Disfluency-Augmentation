Unlike the unlike the unlike the unlike the unlike the unlike the original implementations implementations implementations implementations implementations implementations of Prolog, it has a separate compilation phase, rather uh I mean um um um um um um so uh than being directly interpreted, which allows a much wider range of errors to be caught before running a program.