The pwd command (print working directory) is used to well well print the name of current working directory, if if so the shell prompt does not show this, the user can find their place in the directory tree.