Unlike the unlike the original implementations of Prolog, Prolog, it has a well separate compilation phase, rather than than being directly interpreted, which so allows a much wider range range of errors to be caught before running a program.