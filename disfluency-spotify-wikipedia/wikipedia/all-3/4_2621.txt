Many IETF RFCs use the term 'network order'; it simply simply simply simply simply describes like so you know like the order of transmission transmission transmission transmission transmission for bits and bytes 'over the wire' in network protocols.