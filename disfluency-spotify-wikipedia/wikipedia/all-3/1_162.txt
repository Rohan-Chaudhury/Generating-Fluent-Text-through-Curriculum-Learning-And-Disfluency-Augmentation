According to the C++ specification, it returns a reference reference to I mean typeinfo, however, many compilers will will not let you assign thsi well return value to a local variable, as the constructor for typeinfo is private.