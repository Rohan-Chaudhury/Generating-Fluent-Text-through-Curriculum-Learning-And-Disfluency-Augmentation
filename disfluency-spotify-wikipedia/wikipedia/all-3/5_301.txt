Allocating frequently used variables to registers can be be be be be be critical okay so okay uh like to a program's performance; this this this this this this register allocation is performed either um you know well well okay okay okay okay okay okay by a compiler in the code generation phase, or manually by an assembly language programmer.