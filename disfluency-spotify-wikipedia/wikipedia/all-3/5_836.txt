Data input required intermediate processing via punched paper tape or card and separate separate separate separate separate separate input to a repetitive, labor intensive task, removed from user control and error - prone.