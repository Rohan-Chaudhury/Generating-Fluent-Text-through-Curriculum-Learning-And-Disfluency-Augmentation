The graph of the exponential distribution, called the probability so so so so so so so I mean um okay um you know density function (PDF), (PDF), (PDF), (PDF), (PDF), (PDF), (PDF), is specified in terms of lambda (events per unit time) and x (time).